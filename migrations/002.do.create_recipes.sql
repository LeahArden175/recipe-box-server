CREATE TABLE recipes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    date_created TIMESTAMPTZ DEFAULT now() NOT NUll,
    user_id INTEGER
        REFERENCES recipe_box_users(id) ON DELETE CASCADE NOT NULL
);